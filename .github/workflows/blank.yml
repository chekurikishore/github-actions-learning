name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: "Hello there!"
      - name: pirnt goodbye
        run: "done, bye!"
  
